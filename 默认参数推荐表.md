# ffmpeg 默认参数推荐表（自动码率 → 程序默认值）

> 用途：为「MP4 与源文件相同」类预设的智能 copy 逻辑提供默认配置（源/目标编码一致且码率自动 → copy，否则用目标编码的默认配置）。
> 数据来源：本机 ffmpeg 8.1.2 full build 实测 + ffmpeg 官方文档默认值 + 用户最终确认。

## 一、视频编码器默认推荐

| 编码器 | ffmpeg 自身默认 | **程序默认参数**（自动码率） |
|---|---|---|
| libx264 | CRF 23（实测字节数与 `-crf 23` 完全一致） | **CRF 23** |
| libx265 | CRF 28（实测字节数与 `-crf 28` 完全一致） | **CRF 28** |
| h264_nvenc | 恒定质量 CQ（实测约 889kbps@320x240） | **CQ 26** |
| hevc_nvenc | 恒定质量 CQ（实测约 845kbps@320x240） | **CQ 30** |
| h264_qsv / hevc_qsv | global_quality | **global_quality 23** |
| h264_amf / hevc_amf | qp | **qp 23** |
| libvpx-vp9 | CRF 模式（默认 31） | **CRF 30** |
| libaom-av1 | CRF 模式（默认约 30–32） | **CRF 30** |
| libsvtav1 | 默认质量档（实测约 322kbps） | **CRF 30** |
| mpeg4 / libxvid | qscale（实测约 475kbps） | **`-q:v 3`** |
| mpeg2video | qscale | **`-q:v 3`** |
| wmv2 / wmv3 | 默认质量档（实测约 480kbps） | **`-q:v 5`** |
| mjpeg | qscale | **`-q:v 3`** |
| gif | 默认 256 色（无损） | —（无码率参数） |
| png / bmp / tiff | 无损 | —（无码率参数） |
| libwebp | quality 默认 75 | —（保持默认） |
| ffv1 | 无损 | —（无码率参数） |
| cfhd | 中间格式 | —（保持默认） |

## 二、音频编码器默认推荐

| 编码器 | ffmpeg 自身默认 | **程序默认参数**（自动码率） |
|---|---|---|
| aac（原生） | 默认 bitrate≈128k（立体声） | **192k** |
| libmp3lame | 默认 128k | **192k** |
| libopus | 默认自动码率 | **160k** |
| libvorbis | 默认 -q:a 3 | **160k** |
| ac3 | 默认 96k | **192k** |
| wmav2 | 默认 128k | **192k** |
| flac / alac / pcm | 无损 | —（无码率参数） |

## 三、目标容器 → 默认编码器与码率（智能 copy 落点）

「与源文件相同」预设的目标容器如果没有显式指定编码，按此表决定默认视频/音频编码器与码率：

| 容器 | 默认视频编码 | 默认音频编码 | 默认音频码率 |
|---|---|---|---|
| MP4 / MOV / M4V / F4V | h264（libx264，CRF 23） | aac | 192k |
| MKV | h265（libx265，CRF 28）；兼容优先时 h264（CRF 23） | aac | 192k |
| AVI | mpeg4（`-q:v 3`） | libmp3lame | 192k |
| WMV | wmv2（`-q:v 5`） | wmav2 | 192k |
| WebM | libvpx-vp9（CRF 30） | libopus | 160k |
| MPEG / MPG / VOB | mpeg2video（`-q:v 3`） | libmp3lame | 192k |
| TS / M2TS | h264（CRF 23） | aac 或 ac3 | 192k |
| FLV | h264（CRF 23） | aac | 192k |
| 3GP | h264（CRF 23） | aac | 192k |
| OGV | theora | libvorbis | 160k |
| GIF | gif | （无） | — |
| 图片类（PNG/JPG/TIFF/...） | 对应无损/默认 | （无） | — |

## 四、智能 copy 判定规则

对「MP4 与源文件相同」类预设（目标容器已知、编码未指定）：

1. **视频流**
   - 输入视频编码 == 目标容器默认视频编码（如都是 h264），且码率模式=自动 → `-c:v copy`；
   - 否则 → 用目标默认视频编码转码，码率模式=自动时按「程序默认参数」输出（如 wmv3→h264 → `-c:v libx264`，自动 → 隐式 CRF 23）。
2. **音频流**
   - 输入音频编码 == 目标默认音频编码（如都是 aac），且音频码率=自动 → `-c:a copy`；
   - 否则 → 用目标默认音频编码转码，自动码率时按「程序默认参数」（如 ac3→aac → `-c:a aac -b:a 192k`）。
3. 无音频轨 → `-an`；硬件编码勾选时目标默认编码器替换为对应 GPU 版（libx264 → h264_nvenc 等）。

## 五、预设自动码率触发条件

上述「程序默认参数」只在以下情况生效：
- 用户编辑界面把码率模式选为「自动」**且**预设本身没指定固定码率；或
- 「MP4 与源文件相同」类预设按第四节判定需要转码时；或
- 目标格式预设中未指定具体码率（码率=自动）。

如预设带 `VideoBitrate="8000k"` 等固定码率，按现有「固定码率」模式输出 `-b:v 8000k`，**不套用程序默认**。